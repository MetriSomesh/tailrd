# Copy to .env for local development. Never commit .env.

# ---- Core ----
ENVIRONMENT=local
DEBUG=true
LOG_JSON=false
LOG_LEVEL=INFO

# ---- URLs ----
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:3000

# ---- Security ----
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=dev-only-change-me-0000000000000000000000000000000000
COOKIE_SECURE=false

# ---- Database ----
# Local dev uses SQLite. Production must use PostgreSQL:
#   postgresql+psycopg://user:pass@host/db
DATABASE_URL=sqlite+aiosqlite:///./tailrd_dev.db

# ---- Redis ----
# Set REDIS_FAKE=true to run without a Redis server (dev only).
REDIS_URL=redis://localhost:6379/0
REDIS_FAKE=true

# ---- Google OAuth (optional in dev) ----
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# ---- Email ----
# console = print emails to stdout. resend = real delivery.
EMAIL_PROVIDER=console
# RESEND_API_KEY=
EMAIL_FROM=Tailrd <no-reply@tailrd.local>

# ---- Storage ----
# local = filesystem. s3 = AWS S3.
STORAGE_BACKEND=local
STORAGE_LOCAL_DIR=./_storage
# S3_BUCKET=
# S3_REGION=ap-south-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ---- Payments ----
# mock = deterministic, no network. razorpay = live integration.
PAYMENT_PROVIDER=mock
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# RAZORPAY_WEBHOOK_SECRET=

# ---- Agent ----
# mock = deterministic stub. opencode = real Hermes/OpenCode subprocess.
AGENT_BACKEND=mock
AGENT_COMMAND=opencode
AGENT_TIMEOUT_SECONDS=180

# ---- Pipeline ----
TARGET_SCORE=70
MAX_ITERATIONS=3

# ---- Worker ----
WORKER_ENABLED=true

# ---- Observability ----
# SENTRY_DSN=
