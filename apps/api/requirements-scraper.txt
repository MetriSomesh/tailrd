# Optional: headless-browser fallback for JS-rendered job postings (Tier 2).
# Only needed on hosts where JD_SCRAPE_BROWSER_FALLBACK=true.
# After installing, run: playwright install --with-deps chromium
playwright==1.56.0
